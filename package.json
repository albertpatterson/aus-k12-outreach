{
  "name": "aus-k12-outreach",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run --workspace app dev",
    "build-data": "npm run --workspace data run",
    "build-app": "npm run --workspace app build",
    "build-all-v1": "npm run --workspace data run && npm run --workspace app dev",
    "build-all": "npm run build-data && npm run build-app",
    "deploy": "export GOOGLE_APPLICATION_CREDENTIALS=\"/Users/personal/code/expr/aus-k12-outreach/secret/aus-k12-outreach-key.json\" && firebase deploy",
    "build-all-deploy": "npm run build-all && npm run deploy"
  },
  "workspaces": [
    "app",
    "data"
  ],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "vite": "^7.1.2"
  }
}
